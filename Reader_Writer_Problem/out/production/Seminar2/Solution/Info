This Implementation uses ReentrantLock to manage readers and writers.
It locks or releases thread on arrival policy based on fairness (duration of  wait).
It essentially deals with the starvation problem from the first implementation of the task.